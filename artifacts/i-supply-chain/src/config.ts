// Lead/booking submissions are proxied through the API server (see
// /api/leads and /api/notify routes). Webhook URLs are held server-side
// and must never be exposed in this client bundle.
export {};
